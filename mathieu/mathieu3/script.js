/*var main = function(){
	$('#selection').submit(function(ev) {
    ev.preventDefault(); // to stop the form from submitting
    Validations go here 
    this.submit(); // If all the validations succeeded
});
};

$(document).ready(main);*/